# Copy to `.env.local` (which is git-ignored) to point your local dev server
# at the live Cloudflare Worker. This value is a PUBLIC endpoint, not a secret.
VITE_AI_API_URL=https://kulveer-ai.kulveer-ai.workers.dev
